defaults:
  - main
  - override /dataset: branin
  - override /env: grid
  - override /oracle: branin

logger:
  do:
    online: False
    times: False
  project_name: test_logger
  run_name: ${env.length}_${sampler.id}_${surrogate.id}_${acquisition.id}
  # Log dir
  logdir:
    root: ./logs
    ckpts: ./ckpts
    overwrite: True
  debug: True
  progress: True
  tags: 
      - branin
      - al

dataset:
  train_path: ~/activelearning/data/branin/data_10_train.csv
  
env:
  # Number of cells per dimension
  length: 10
  # Mapping coordinates
  cell_min: -1
  cell_max: 1

device: cpu
float_precision: 32
budget: 10
n_samples: 5
seed: 31415
maximize: False
