_target_: model.regressor.DropoutRegressor

# Device
device: cuda

# Path
path_model: "./storage/model_proxy/proxy"

# Training
training:
  eps : 1.0e-5
  dropout : 0.1
  max_epochs : 100
  training_batch : 8
  history : 25
  learning_rate: 1e-3
  weight_decay: 0.0